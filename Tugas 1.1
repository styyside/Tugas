#include <stdio.h>
int angka;


int main() {
    printf("Masukan angka = "); 
    scanf("%d", &angka); // ("%d")
    if(angka > 10){
        printf("adalah angka anda %d", angka); //(;)
        return 0;} //(})
}

